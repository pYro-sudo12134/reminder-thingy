apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets-dir
  namespace: database
type: Opaque
stringData:
  POSTGRES_USER: "${POSTGRES_USER:-postgres}"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
  POSTGRES_DB: "voice_reminder"
  postgres_user.txt: "${POSTGRES_USER:-postgres}"
  postgres_password.txt: "${POSTGRES_PASSWORD}"
  postgres_db.txt: "voice_reminder"