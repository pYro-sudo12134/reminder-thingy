apiVersion: v1
kind: Service
metadata:
  name: opensearch-exporter
  namespace: monitoring
  labels:
    app: opensearch-exporter
    component: exporter
spec:
  selector:
    app: opensearch-exporter
  ports:
    - port: 9114
      targetPort: 9114
      name: metrics
  type: ClusterIP