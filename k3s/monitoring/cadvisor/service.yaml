apiVersion: v1
kind: Service
metadata:
  name: cadvisor
  namespace: monitoring
  labels:
    app: cadvisor
    component: metrics
spec:
  selector:
    app: cadvisor
  ports:
    - port: 8080
      targetPort: 8080
      name: metrics
  type: ClusterIP