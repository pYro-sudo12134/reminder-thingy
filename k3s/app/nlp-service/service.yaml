apiVersion: v1
kind: Service
metadata:
  name: nlp-service
  namespace: app
  labels:
    app: nlp-service
    component: nlp
spec:
  selector:
    app: nlp-service
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
  type: ClusterIP