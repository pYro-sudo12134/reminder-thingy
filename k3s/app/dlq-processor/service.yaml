apiVersion: v1
kind: Service
metadata:
  name: dlq-processor
  namespace: app
  labels:
    app: dlq-processor
    component: processor
spec:
  selector:
    app: dlq-processor
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  type: ClusterIP